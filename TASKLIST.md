# TASKLIST

Audit-only registry for the repository-wide forensic review. This file tracks validated issues and items that still require live Supabase or browser/device access. No application code changes are included in this phase.

| ID | Severity | Issue | Root Cause | Affected Files | Proposed Fix | Validation Steps | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AUD-001 | Critical | Call signaling accepts untrusted consultation and sender identifiers | `api/calls.js` uses the Supabase service role and writes `call_signals` from client-supplied `consultationId` and `senderId` without resolving the authenticated user or verifying consultation membership/state | `api/calls.js`, `api/_runtime.js`, `supabase/migrations/012_call_signaling.sql`, `supabase/migrations/015_call_ring_signal.sql` | Add bearer-token auth on all call-signal reads/writes, resolve the actor server-side, verify consultation ownership, and enforce consultation/payment state before ring/offer/answer/candidate/leave actions | Static review of runtime handler and RLS bypass path; add live API auth tests against valid and invalid consultation IDs in a later execution phase | validated |
| AUD-002 | Critical | Calls disconnect automatically when meeting view unmounts | `MeetingPage` always emits a `leave` signal in cleanup, so navigation/state transitions are indistinguishable from an intentional hang-up | `src/components/MeetingPage.tsx` | Emit `leave` only on explicit hang-up or confirmed tab-close intent; make component cleanup local-only unless the user actually ends the call | Static review of `useEffect` cleanup and call-end flow; verify with future paired-call test that navigation away from the component no longer disconnects the remote peer | validated |
| AUD-003 | Critical | Calls stall on “Calling Lawyer” or never connect on many networks | WebRTC peer setup is STUN-only, has no TURN relay, and relies on polling-only signaling with no ICE restart/recovery strategy | `src/components/MeetingPage.tsx`, `src/api.ts`, `api/calls.js` | Provide TURN credentials, add ICE restart/timeouts, and harden signaling delivery/ordering | Static review of ICE server config and signaling flow; validate later with real cross-network browser/device tests | validated |
| AUD-004 | High | Call polling repeatedly re-reads the same trailing signal window | The `since` cursor refs are initialized once and never advanced after successful fetches; de-duplication is in-memory only | `src/components/MeetingPage.tsx`, `src/components/ChatPage.tsx` | Advance cursors after each successful fetch or replace polling with realtime delivery | Static review of polling refs; future runtime check should confirm incremental fetch behavior | validated |
| AUD-005 | Critical | Client can be forced out of consultation flow prematurely | Leaving chat as a client marks the consultation `completed`, which can end access even when the user only navigated away temporarily | `src/App.tsx`, `api/consultations/status.js`, `src/api.ts` | Separate “leave current screen” from “complete consultation”; only complete through an explicit end-session workflow | Static review of `leaveConsultationView()` and status update path; verify later that back-navigation no longer changes consultation state | validated |
| AUD-006 | High | Lawyer/frontend consultation access is overly broad | Frontend access control allows lawyers into chat/meeting views generically rather than scoping access to a specific consultation | `src/utils/accessControl.ts`, `src/App.tsx` | Require consultation-scoped access checks for lawyer meeting/chat entry points, not just role-based allowance | Static review of view-gating logic; verify later with a lawyer account that unrelated consultations cannot be opened | validated |
| AUD-007 | Critical | Login and registration can overwrite lawyer profile data with placeholders | Auth bootstrap upserts `profiles`, `lawyer_profiles`, and `lawyer_directory` with default fields during sign-in/registration | `src/supabaseAuth.ts`, `api/auth/register.js`, `supabase/migrations/003_auth_profile_trigger.sql`, `supabase/migrations/013_auto_verify_lawyers.sql` | Remove destructive auth-time upserts; move lawyer provisioning/verification to controlled server-side workflows | Static review of auth-time writes; live verification later should confirm repeated login does not reset real lawyer fields | validated |
| AUD-008 | High | Lawyer verification workflow is internally inconsistent | UI implies pending admin verification, but runtime registration/bootstrap marks lawyers active and verified immediately | `src/components/RegisterPage.tsx`, `src/supabaseAuth.ts`, `api/auth/register.js` | Align UI and backend around one verification model; if manual approval is intended, lawyers must remain pending until approved | Static comparison of UI copy versus backend behavior; later test should confirm newly registered lawyers stay pending if that model is retained | validated |
| AUD-009 | Critical | Mixed Supabase schema paths create drift and migration instability | The repo supports legacy tables, `app_*` compatibility resources, and normalized v2 resources simultaneously, with behavior depending on environment migration history | `src/api.ts`, `api/_runtime.js`, `supabase/migrations/017_rusdi_and_toliver_schema_v2.sql`, `supabase/migrations/024_manual_payment_system.sql`, `supabase/migrations/025_production_stabilization.sql`, `supabase/migrations/026_unified_production_fix.sql` | Freeze one canonical production schema path, inventory the live project state, and remove conflicting fallback paths after migration validation | Static migration/code review complete; live schema inventory still required before any cleanup | validated |
| AUD-010 | Critical | Migration history contains destructive table/view resets | Migration 017 drops core tables, views, triggers, and types before recreating a new model, which is unsafe for live data without explicit migration/backup controls | `supabase/migrations/017_rusdi_and_toliver_schema_v2.sql` | Treat 017 as destructive; verify whether it ever ran in production and restore or transform data before reusing it | Static migration review complete; production execution history requires live environment evidence | needs-live-env |
| AUD-011 | High | Chat runtime bypasses RLS protections via service-role access | `api/chat.js` creates sessions and messages through service-role REST calls and validates only payment state for some actors, not full participant identity | `api/chat.js`, `api/_runtime.js`, `supabase/migrations/005_operational_features.sql`, `supabase/migrations/010_ensure_chat_runtime.sql` | Add authenticated runtime authorization matching consultation participants before session creation or message send/list operations | Static review of chat runtime path complete; live actor-isolation tests still needed | validated |
| AUD-012 | High | Payment proof upload auto-verifies payments despite manual-verification schema | `upload-proof` immediately marks payment `paid` and unlocks the consultation, bypassing the separate lawyer/admin verification workflow | `api/payments.js`, `api/payments/verify.js`, `supabase/migrations/024_manual_payment_system.sql`, `supabase/migrations/025_production_stabilization.sql`, `supabase/migrations/026_unified_production_fix.sql` | Choose one verification model; if manual verification is intended, proof upload should move to `waiting_verification` instead of `paid` | Static review of upload and verification flows complete; future end-to-end payment test should confirm final workflow behavior | validated |
| AUD-013 | Medium | Invoice total calculation is inconsistent with stored platform fee | Payment creation computes and stores `platform_fee` but does not include it in `totalAmount` | `api/payments.js`, `api/_runtime.js` | Reconcile total computation and invoice presentation with the intended fee model | Static calculation review complete; later business-rule confirmation required before implementation | validated |
| AUD-014 | Medium | Payment proofs are stored in a public bucket | `payment-proofs` is configured as public in the production-fix migration, which exposes sensitive payment artifacts more broadly than necessary | `supabase/migrations/026_unified_production_fix.sql`, `api/payments.js` | Make proofs private and serve via authenticated or signed access instead of public URLs | Static review complete; live bucket policy verification required before rollout changes | validated |
| AUD-015 | Medium | Rusdi AI persistence is fragmented across multiple fallback tiers | The app falls back from `ai_conversations`/`ai_messages` to `ai_chat_history` and then to local storage, which preserves availability but fragments auditability | `src/services/chatService.ts`, `src/pages/RusdiPage.tsx`, `supabase/migrations/025_production_stabilization.sql`, `supabase/migrations/026_unified_production_fix.sql` | Standardize on one persistence model and add explicit telemetry when fallback tiers are used | Static review of fallback chain complete; live DB availability tests can validate the current degradation behavior later | validated |
| AUD-016 | Medium | Meeting and chat layouts are desktop-first and high-risk on small mobile screens | `MeetingPage` uses full-screen fixed composition and absolute overlays; chat/meeting surfaces are not designed around constrained mobile video layouts | `src/components/MeetingPage.tsx`, `src/components/ChatPage.tsx` | Preserve current design language but add a dedicated mobile meeting/chat layout in a later implementation phase | Static responsive audit complete; real device/browser validation still needed | validated |
| AUD-017 | Medium | Expo/mobile reuse is blocked by browser-bound domain code | Meeting/auth/AI flows depend directly on DOM refs, browser media APIs, `window`, and `localStorage`, limiting clean reuse in React Native/Expo | `src/components/MeetingPage.tsx`, `src/supabaseAuth.ts`, `src/services/chatService.ts`, `src/App.tsx` | Extract platform-neutral domain/service layers and move browser-specific adapters behind interfaces before mobile implementation | Static architecture review complete; no code changes in this phase | validated |
| AUD-018 | Medium | Cross-browser and multi-device call behavior cannot be proven from repo-only audit | Safari/iOS/Android permissions, autoplay policies, network handoffs, and NAT traversal need live paired-device testing | `src/components/MeetingPage.tsx`, `src/components/ChatPage.tsx` | Run a live browser/device certification matrix after signaling and TURN hardening | Static analysis only in this phase; schedule real-device tests later | needs-live-env |
| AUD-019 | Medium | Live Supabase integrity cannot be fully verified from repo alone | Orphan counts, actual physical tables vs compatibility views, bucket state, and migration execution order in the live project need direct environment access | `supabase/migrations/*`, `scripts/dev/audit-runtime.cjs`, `src/api.ts`, `api/_runtime.js` | Run a live schema/resource integrity audit against the actual Supabase project before any cleanup or migration rollout | Repo-level review complete; blocked on live environment credentials/state | needs-live-env |

## Validation Snapshot
- `npm.cmd run build`: passed on 2026-06-16
- `npm.cmd run lint`: passed on 2026-06-16
- Cross-browser/device WebRTC validation: not executed in this phase
- Live Supabase integrity validation: not executed in this phase
